version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.16.3
    steps:
      - checkout
      - run:
        name: eslint
        command: npm run lint
      - run:
        name: test
        command: npm run test
